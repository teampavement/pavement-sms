const setReminderAPI = (body) => {
  /*
  here, have to figure out when canPark becomes false

  strategy is:
    - take current time as starting point
    - iterate 15 minutes at a time
    - call canPark until it returns false
    -
  */
  return true;
}

export default setReminderAPI;
